const PREFIX = 'ColorPickerForm';

const styles = {
  [`& .${PREFIX}-picker`]: {
    width: "100% !important",
    marginTop: "2rem"
  },
  [`& .${PREFIX}-addColor`]: {
    width: "100%",
    padding: "1rem",
    marginTop: "1rem",
    fontSize: "2rem"
  },
  [`& .${PREFIX}-colorNameInput`]: {
    width: "100%",
    height: "70px"
  }
};

export default styles;